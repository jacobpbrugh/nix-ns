[package]
name = "nix-ns"
version = "0.1.0"
edition = "2024"

[dependencies]
anyhow = "1.0.98"
clap = { version = "4.5.40", features = ["derive"] }
libc = "0.2"
nix = { version = "0.30.1", features = ["mount", "sched", "user", "fs"] }
tempfile = "3.20.0"
